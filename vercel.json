{
  "version": 2,
  "buildCommand": "cd server && npm install && cd ../frontend && npm install && npm run vercel-build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/:path*", "destination": "/index.html" }
  ]
}

